<?xml version="1.0"?>
<!--
/**
 * Copyright © Elgentos. All rights reserved.
 * https://elgentos.nl
 */
-->
<schema xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:noNamespaceSchemaLocation="urn:magento:framework:Setup/Declaration/Schema/etc/schema.xsd">
    <table name="elgentos_serversideanalytics_sales_order" engine="innodb" comment="Sales orders">
        <column xsi:type="int" name="id" padding="10" unsigned="true" nullable="false" identity="true" comment="Id"/>

        <column xsi:type="int" name="order_id" padding="10" unsigned="true" nullable="false" comment="Order id"/>

        <column xsi:type="text" name="ga_user_id" comment="Google Analytics User Id"/>
        <column xsi:type="text" name="ga_session_id" comment="Google Analytics Session Id"/>

        <constraint xsi:type="primary" referenceId="PRIMARY">
            <column name="id"/>
        </constraint>

        <!-- In order to enforce a 1:1 relation, create a unique constaint. -->
<!--        <constraint xsi:type="unique" referenceId="ELGENTOS_SERVERSIDEANALYTICS_SALES_ORDER_ORDER_ID">
            <column name="order_id"/>
        </constraint>-->

        <!-- Create a foreign key constraint to the related table to be related, which in this case is sales_order. -->
<!--
        <constraint xsi:type="foreign" referenceId="ELGENTOS_SERVERSIDEANALYTICS_SALES_ORDER_ORDER_ID_SALES_ORDER_ENTITY_ID" table="elgentos_serversideanalytics_sales_order" column="order_id" referenceTable="sales_order" referenceColumn="entity_id" onDelete="CASCADE"/>
-->
    </table>
</schema>
